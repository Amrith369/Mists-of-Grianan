using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enemyIdle : MonoBehaviour {

	private Vector3 FinalTemp;
	public float TempDistance = 10f;
	public float range;
	public float unitcircle;
	public float Temp;

	// Use this for initialization
	void Start () {
		/*&
		Temp = random.unitcircle;
		TempDistance = random.range(0, 10);
		Temp = Temp * TempDistance;
		FinalTemp = new vector3(Temp.x, 0, Temp.y);
	}
	
	// Update is called once per frame
	void Update () {
		MovePos = Vector3(transform.position + FinalTemp);
	}*/
}
}
